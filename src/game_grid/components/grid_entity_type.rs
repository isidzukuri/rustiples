#[derive(Debug, Eq, PartialEq, Hash, Copy, Clone)]
pub enum GridEntityType {
    Castle,
    Tree,
    Axe,
    Hero,
    Mountain,
    Water,
    LumberMill,
    Bridge,
    Mineral,
    // Standard,
    // Blocked,
    // RouteHead,
    // RoutePoint,
}
